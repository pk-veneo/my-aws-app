# Core dependencies for Training Roster Automation Lambda function
# AWS SDK for Python (boto3) - included in Lambda runtime
# No external dependencies required for basic functionality
# Add additional packages as needed for your specific use case

# Example dependencies (uncomment and modify as needed):
# requests>=2.28.0
# pandas>=1.5.0
# pydantic>=1.10.0
